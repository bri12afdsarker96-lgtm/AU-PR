# 打包说明（Windows 发行版）

## 一键打包（推荐）

双击仓库根目录的 **`打包.bat`** —— 自动检查 Python/PyInstaller 并完成打包。
产物在 `dist\水星配音对齐工作室\`。

## 手动打包（等价命令）

```powershell
python -m pip install pyinstaller
$env:PYTHONPATH='source'
pyinstaller --noconfirm --clean --onedir --name "水星配音对齐工作室" `
  --paths source `
  --add-data "source\dub_align_studio\web;dub_align_studio\web" `
  --collect-submodules dub_align_studio --collect-submodules integrated_workbench `
  --console source\dub_align_studio\launcher.py
```

## 打包后三步

1. **ffmpeg**：把 `ffmpeg.exe` / `ffprobe.exe` 放到 exe 同目录（或系统 PATH）。
2. **数据总目录**：首次启动 → 「工具箱自检」页设置总目录（建议非 C 盘，如
   `D:\水星配音数据`）→ 点「🩺 环境自检」。组件/模型/音色库/克隆音频/字体
   全部存于该目录，整个文件夹可拷到其他电脑直接使用，已存在的不会重复下载。
3. **复用已下载模型**：把旧的 `whisper.cpp` 文件夹整个移入 `总目录\组件\`，
   点环境自检即识别。

## 说明

- 入口固定为 `source\dub_align_studio\launcher.py`（用 `__main__.py` 打包会闪退）。
- 保留 `--console` 便于看服务日志；启动异常会写 exe 旁「启动错误.log」。
- 安装包不内嵌任何模型/重资产（轻量安装纪律），全部由软件内工具箱按需下载。
