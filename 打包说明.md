# 打包说明（Windows 发行版）

## 一键打包（推荐）

双击仓库根目录的 **`打包.bat`** —— 自动检查 Python/PyInstaller 并完成打包。
产物在 `dist\水星配音对齐工作室\`。

## 手动打包（等价命令）

```powershell
python -m pip install pyinstaller
$env:PYTHONPATH='source'
pyinstaller --noconfirm --clean --onedir --name "水星配音对齐工作室" `
  --paths source `
  --add-data "source\dub_align_studio\web;dub_align_studio\web" `
  --collect-submodules dub_align_studio --collect-submodules integrated_workbench `
  --console source\dub_align_studio\launcher.py
```

## 常见失败：拒绝访问（PermissionError WinError 5）

打包要清空重建 `dist\水星配音对齐工作室\`，该目录被占用就会「拒绝访问」。
新版 `打包.bat` 已自动处理大部分情况（自动结束旧进程 + 预清理检查）；仍失败时依次检查：

1. 关闭正在运行的软件窗口（包括最小化的黑色控制台窗口）；
2. 关闭正打开着 `dist\水星配音对齐工作室` 的资源管理器窗口；
3. 暂停杀毒软件实时扫描后重试。

## ⚠ 数据总目录不要放在 dist 里

`dist\` 每次打包都会被**整个删除重建**。如果软件里把数据总目录设成了
`dist\水星配音对齐工作室\水星配音数据`，里面下载的模型/字体/音色会在下次打包时丢失。
新版 `打包.bat` 检测到这种情况会先把整个数据文件夹搬到仓库根目录保护起来，
打包完成后在「工具箱自检」页把数据总目录指向新位置（选择 → 保存 → 环境自检）即可。
**建议**：数据总目录设在仓库外，例如 `D:\水星配音数据`。

## 带版本号的压缩包（自动生成）

`打包.bat` 打包并自检通过后，会自动把成品压成一个带版本号+时间戳的 zip，输出到仓库根的
`发布包\` 目录，例如：

```
发布包\水星配音对齐工作室_v0.7.1_20260723_1530.zip
```

文件名含版本号与打包时间，**多次打包一眼可分**，不会再搞混哪个是新版。zip 内是
`水星配音对齐工作室\` 整个产物目录，解压即用。

> 想让压缩包自带 ffmpeg：先把 `ffmpeg.exe` / `ffprobe.exe` 放进 `dist\水星配音对齐工作室\`
> 再运行一次打包（或手动压缩该文件夹）。`发布包\` 与 `*.zip` 已加入 .gitignore，不进仓库。

## 打包后三步

1. **ffmpeg**：把 `ffmpeg.exe` / `ffprobe.exe` 放到 exe 同目录（或系统 PATH）。
2. **数据总目录**：首次启动 → 「工具箱自检」页设置总目录（建议非 C 盘，如
   `D:\水星配音数据`）→ 点「🩺 环境自检」。组件/模型/音色库/克隆音频/字体
   全部存于该目录，整个文件夹可拷到其他电脑直接使用，已存在的不会重复下载。
3. **复用已下载模型**：把旧的 `whisper.cpp` 文件夹整个移入 `总目录\组件\`，
   点环境自检即识别。

## 说明

- 入口固定为 `source\dub_align_studio\launcher.py`（用 `__main__.py` 打包会闪退）。
- 保留 `--console` 便于看服务日志；启动异常会写 exe 旁「启动错误.log」。
- 安装包不内嵌任何模型/重资产（轻量安装纪律），全部由软件内工具箱按需下载。
