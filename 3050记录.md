# 3050 台式机 · 环境与操作记录

> 这台电脑无 git（`git` 报 WinError 2），通过下载仓库 ZIP 同步；操作日志由本人上传，Claude 代记到本分支。

## 2026-07-26 · dots.tts 依赖修复完成 ✅

- Python：`D:\Python\Python311\python.exe`（3.11.9，python.org 版，非 Store）
- torch：`2.11.0+cu126`
- torchaudio：`2.11.0+cu126`（与 torch 同版本，已匹配）
- transformers：`4.57.0`，accelerate：`1.12.0`，dots-tts：`0.2.1`
- CUDA 可用：True，GPU：**NVIDIA GeForce RTX 3050**（显存 7.0/8.0 GB 可用）
- **dots.tts probe：available = True**（主配音引擎就绪，可正常配音出片）

修复经过：初装时 `torch 2.13.0` 但缺 `torchaudio` → dots.tts 报 `No module named 'torchaudio'`；
经工具箱/`本次操作.bat` 按 cu126 补齐并把 torch 锁到与 torchaudio 同版本（2.11.0+cu126）后就绪。

### 备注
- fish-speech（备选引擎）安装失败可忽略：它硬锁 `torch==2.8.0`，装上反而会顶掉 dots.tts 需要的 cu126 torch。主引擎 dots.tts 已够用。
- 渲染成片仍需 `ffmpeg.exe`/`ffprobe.exe` 放到软件目录或系统 PATH。
- 如需 `本次操作.bat` 能自动把日志推回本分支：在这台机装 **Git for Windows** 并用 `git clone` 取代 ZIP 下载。

## 2026-07-26 · 迁移到干净路径 D:\Mercury 完成 ✅

- 仓库：`D:\Mercury\AU-PR`（新克隆）；数据：`D:\Mercury\水星配音数据`（已从旧 AU&PR 路径搬来）
- **彻底摆脱路径里的 `&`**（根治字幕 □□□ / 脚本路径截断 / ffmpeg 字体加载失败）
- 迁移后两处收尾已修：
  - torch 曾被 fish-speech 依赖降级成 2.8.0(CPU) → 强制装回 **2.11.0+cu126**（+ torchaudio 2.11.0+cu126）
  - 数据总目录旧设置指向已删路径 → `set_data_root` 重指到 `D:\Mercury\水星配音数据`
- 验证：cuda=True（RTX 3050）、data_root=`D:\Mercury\水星配音数据`、**dots.tts available=True**
- 遗留：这台未装 git（快照 git 报 WinError 2），靠 GitHub Desktop / ZIP 同步；不影响使用
- ⚠ 勿再点 fish-speech「安装」——会把 torch 再次降级成 2.8.0 CPU
- 待办：确认无误后删旧目录 `rmdir /s /q "D:\GitHub\By\AU&PR"`

## 2026-07-26 · 三文件夹合并为一、清理完成 ✅
- 已删除旧目录 `D:\GitHub\By\AU&PR`（含 3060 拷贝 + 嵌套克隆 + 旧 dist）。
- 最终只剩：`D:\Mercury\AU-PR`（代码）+ `D:\Mercury\水星配音数据`（数据/模型）。
- 验证：torch 2.11.0+cu126 / dots.tts available=True / RTX 3050 / data_root=D:\Mercury\水星配音数据。
- 3050 环境彻底就绪、路径无 &、目录清爽。后续常规同步用 GitHub Desktop（这台未装命令行 git）。
